# 标签超市项目 · 开发历程

**一句话定位**：本地优先的 NovelAI V5 提示词标签超市——FastAPI（8123）标签/搜索/图库底座 + Node（8787）官方 API 生图代理层 + 原生 JS 前端，覆盖「搜标签 → 选标签 → 整理 Prompt → 生图 → 图库闭环」的完整工作台。

本文档为里程碑式项目历史记录。时间线以真实 git 提交数据校准（git 历史覆盖 **2026-08-24 ~ 2026-08-26**，共 18 个提交）；早于 git 首提交的阶段依据文件时间戳与数据资产推断，已明确标注。

---

## 时间线总览

| 阶段 | 时间 | 主题 | 关键产出 |
| --- | --- | --- | --- |
| ① 项目起源与早期积累 | ~2026-06（推断） | 收集 NovelAI 历史图包，为图库/例图做素材积累 | `data/nai_packages/` 图包 zip（最早 2026-06-13）；`data/gallery/` 解包目录 |
| ② 骨架落地与例图基础 | 2026-08-24 | 初始导入 FastAPI + SQLite 标签超市骨架；优化例图加载与热门预缓存 | 初始提交（app.py / db.py / search.py / importer/ / prompt/ / static/）；例图懒加载与预缓存 |
| ③ V2 基线与 NovelAI 生图链路 | 2026-08-25 | 完成 V2 全量基线，落地 Node 生图代理层；完善官方生图链路、标签目标选择、档位与本地翻译基础；macOS launchd 常驻与诊断工具 | Node `server/` 全量基线；V5 生图链路；正负面档位；`install-launchagent.sh` + 诊断工具；连接/例图/双入口修复轮（Python 86 / Node 19 通过） |
| ④ 搜索增强 / 配置抽离 / API-only 收尾 | 2026-08-26 | 拼音搜索、权重手动输入、分区记忆；抽离本机配置、代码通用化、补 autocomplete；重写 README；移除 CDP/网页兼容层，定稿 API-only 运行时契约 | 拼音搜索（pinyin_util / backfill_pinyin）；`.env.example` + 环境变量化；README 全量重写；删除 adapter/cdp/jobs/sync 兼容层 |
| ⑤ 工作区未提交成果（进行中） | 2026-08-26 之后 | Tag Assistant、Visual Prompt Builder、语义导航、PromptDocument schema v2、UI 重构收尾、测试基线扩充 | `static/tag-assistant.js`、`static/visual-builder.js`、`static/prompt-document.js`、`config/prompt_navigation.json`、`/api/catalog/semantic`、`/api/recommendations`、tests 新增 4 个文件；README 基线 Python 134 项 |

> 说明：git 实际首提交为 **2026-08-24**（非 08-23），末提交为 **2026-08-26**。任务材料中「2026-08-23→08-25」的口径与真实 git 输出不符，本文档一律以真实 git 为准。

---

## ① 项目起源与早期积累（约 2026-06，推断）

> 本阶段早于 git 首次提交，依据**文件时间戳与图包资产**推断，无 git 佐证。

**背景**：项目本质是一个「把本地已有的 NovelAI 生图素材变成可搜索、可复用的标签库」的工具。在代码仓库建立之前，本地已经积累了若干 NovelAI 官方导出图包。

**关键动作**（依据资产时间戳推断）：
- 2026-06-13 前后：导出第一批 NovelAI 历史图片包（`data/nai_packages/NAI-images_(2026-06-13_22-41-06)_1-of-1.zip`，约 166 MB，zip 文件 mtime 为 2026-06-13）。
- 2026-07-13 前后：第二批图包导出（`NAI-images_(2026-07-13_23-36-03)_1-of-1.zip`，约 292 MB）。
- 这些图包在 2026-08-24 被解包到 `data/gallery/`（目录 mtime 为 08-24，解包时间晚于导出时间），成为后续「图库」功能的原始素材；第三批图包 `NAI-images_(2026-08-24_05-51-56)` 于 08-24 当天导出并解包（约 461 MB）。
- `data/gallery/nai_generated/`（mtime 08-26）为后续通过本工具生成结果的落盘目录。

**验证情况**：无（本阶段无代码、无测试，仅素材积累）。

---

## ② 骨架落地与例图基础（2026-08-24）

**背景**：正式进入开发。将「标签超市」从想法落成代码：FastAPI + SQLite 后端 + 原生 JS 前端 + 一批数据导入工具。

**关键动作**：
- **初始导入**（commit `826a6ca`「初始导入：NovelAI V5 本地提示词标签超市（FastAPI + SQLite）」）：一次性建立项目骨架——
  - 后端入口 `app.py`、数据底座 `db.py`、搜索排序 `search.py`；
  - 导入清理工具 `importer/`（`sync_danbooru` 增量同步、`import_taxonomy`、`import_danbooru_zh` 中文名、`import_aliases`、`import_restricted` 受限分类、`build_catalog` 目录构建）；
  - Prompt 整理模块 `prompt/`（`composer` 分区合成、`import_parser` 导入解析、`novelai_export` 语法导出）；
  - 例图侧 `precache.py`（热门预缓存）、`import_nai.py`（图包导入）、`imageutil.py`；
  - 前端 `static/`（index.html / app.js / app.css）、模型能力配置 `config/model_overlays.json`；
  - 首批 10 个 Python 测试（`tests/test_alias_resolution.py`、`test_catalog_contract.py`、`test_favorite_persistence.py`、`test_gallery_safety.py`、`test_import_parser.py`、`test_model_overlay.py`、`test_prompt_export.py`、`test_restricted_taxonomy.py`、`test_settings.py`、`test_user_zh.py`）。
- **优化例图加载与热门预缓存**（commit `5ee1980`）：改进 `precache.py` 热门标签预缓存与前端例图加载体验（`static/` 三件套 + `app.py` + `sync_danbooru.py`）。

**验证情况**：初始 10 个 Python 单测随骨架提交；例图预缓存有 `data/precache.log` 运行记录（08-24 18:46 落盘）。

---

## ③ V2 基线与 NovelAI 生图链路（2026-08-25）

**背景**：骨架可用后进入「能用起来」阶段：搜索升级为 V2、打通 NovelAI 生图、统一前端入口、提供 macOS 常驻与诊断手段，并完成一轮「连接失败 + 例图不加载 + 重复导航」的真实问题修复。

**关键动作**：
- **完成 V2 全量基线**（commit `51d308c`）：Node 生图代理层 `server/` 首次整体落地（`server.mjs` 入口、`novelai-provider.mjs` payload 构建、`generation-request.mjs` 请求规范化、`api-batch.mjs` 串行批次、`store.mjs` 资产存储、`start-nai.sh` 启动脚本），同时新增 `prompt/sections.py` 与 `tests/test_v2_backend.py`；此阶段 Node 层仍含 `adapter.mjs / cdp.mjs / sync.mjs / jobs.mjs` 网页兼容模块（后续 08-26 被移除）。
- **完善 NovelAI V5 生图链路与标签目标选择**（commit `9cc4bd0`）：新增前端 `static/prompt-compiler.js`，生图链路适配 V5。
- **macOS launchd 启动方式与诊断工具**（commit `8bcad56`）：新增 `install-launchagent.sh`（LaunchAgent 开机常驻）与 `标签超市-诊断.command`（故障排查）。
- **WebUI 对齐、例图预设与本地翻译基础**（commit `1e517ad`）：后端补例图/翻译相关 API，新增 `tests/test_prompt_compiler.mjs`。
- **正负面提示词档位**（commit `f4dda5d`）：增加正面 `off/standard/light` 与负面 `off/light/heavy/furry_focus/human_focus` 档位选择；后续 08-26 凌晨 `51c0b7b` 扩展档位并修复语义候选滚动、同步测试与 README。
- **修复轮**（TASK_REVIEW_PACKET.local.md，2026-08-25 定稿）：修复 NovelAI 无法连接（账户端点迁移到 `image.novelai.net/user/subscription` 只读探测）、标签例图加载死锁（`loading=lazy` + `display:none` 导致 `onload` 永不触发，改 `opacity` 方案）、双入口统一为 8787（FastAPI 白名单代理）、重复导航合并、官方 API-only 迁移与例图 Anlas Gate（未确认请求返回 428）。

**验证情况**：修复轮 Python **86/86**、Node **19/19** 全部通过；真实本机只读探测返回 `connected`；例图端到端 `naturalWidth>0`；桌面/移动（iPhone 14，390px）无横向溢出，仅五个主入口；验收截图 `outputs/fixed-desktop.png`、`outputs/fixed-mobile.png`。UI 重构收尾概览（`outputs/overview.md`，08-25 10:22）另验证 Python 86 passed、134 个 id 无重复、390×844 移动端实测、交付截图 `outputs/ui-dashboard-final.png`。

---

## ④ 搜索增强 / 配置抽离 / API-only 收尾（2026-08-26）

**背景**：功能补齐期——搜索能力增强、把本机私密细节从仓库剥离、并正式宣告「官方 API-only」运行时契约。

**关键动作**：
- **拼音搜索、权重手动输入与分区记忆**（commit `f66e963`）：新增 `importer/pinyin_util.py` 与 `importer/backfill_pinyin.py`（幂等回填拼音列），`search.py` 支持 `pinyin_exact` / `pinyin_partial` 匹配；购物车权重支持手动输入（面板不自动关闭）；用户分区移动写入 `tag_section_override`，实现分区记忆；新增 `tests/test_pinyin_search.py`。
- **结构化提示词稳定性、target remap 与 modal 导入流程**（commit `3ae9b3c` 英文版与 `66ac753` 中文版，同一主题两笔提交）：修 `static/app.js`、`prompt/novelai_export.py`、`static/index.html` 的导入弹窗与导出稳定性，补 `tests/test_novelai_guard.py`、`tests/test_translate.py`、`test_app_helpers.mjs`。
- **移除本机环境细节**（commit `a2e4bc6`）：清理文档/配置/启动脚本中的本机路径与隐私信息。
- **抽离本机配置，代码通用化并补充 autocomplete**（commit `8c3a58a`）：新增 `.env.example`，用户设置迁至仓库外（`~/.workbuddy/tags-market-settings.json`，可用 `WORKBUDDY_HOME` 覆盖），`app.py` / `start-nai.sh` 环境变量化；前端补搜索 autocomplete（08-26 末 `46282b8` 再修 caret 位置与 workspace 目标同步）。
- **docs：重写 README**（commit `9c51f11`）：全量补全功能与架构说明（即当前 README 主体）。
- **Refactor prompt workspace UI and fix filter binding**（commit `d540bdb`）：重构 Prompt 工作区 UI，修复过滤器绑定。
- **API-only 收尾**（commit `1a52589` + `d11fea7`）：删除 `server/adapter.mjs`、`server/cdp.mjs`、`server/jobs.mjs`、`server/sync.mjs` 网页兼容层；定稿运行时契约——Node 仅依赖 `undici`（代理）+ `jszip`（官方响应解压）两个运行时依赖（`package.json` / `package-lock.json`），统一入口 8787，`/api/status` 返回 `{"mode":"api-only"}`。

**验证情况**：README 提交后记录的基线为 Python 与 Node 全量回归（详见阶段⑤基线口径）；生图不依赖 Edge/CDP/浏览器登录。

---

## ⑤ 工作区未提交成果（进行中，已落地-待提交）

> 以下功能/改动已在工作区落地、可运行，但**晚于 HEAD 最近提交（`d11fea7`）**，尚未提交。来源：`git status` 未提交列表 + README 当前内容 + `outputs/overview.md`。

**未提交文件**：
- 已修改：`README.md`、`app.py`、`static/index.html`、`static/app.js`、`static/app.css`
- 未跟踪新增：`config/prompt_navigation.json`、`static/prompt-document.js`、`static/tag-assistant.js`、`static/tag-assistant.css`、`static/visual-builder.js`、`static/visual-builder.css`、`tests/test_prompt_document.mjs`、`tests/test_recommendations.py`、`tests/test_tag_assistant.mjs`、`tests/test_visual_builder.mjs`、`配置指南.md`

**主要成果**：
- **PromptDocument 数据契约（schema_version=2）**：`static/prompt-document.js` 作为 Prompt 统一契约，`normalize()` 兼容旧数组形态与 v2；`serializeTarget()` 保留 NovelAI `weight::tag::` 语法。
- **语义导航与确定性推荐**：`config/prompt_navigation.json` 定义 Base/Character 两棵创作概念骨架（节点含 `id/label/zh/target/section/nsfw/seed_tags/children`，seed 全部取自本地词库真实标签）；新增路由 `GET /api/catalog/semantic`（支持 `node_id` 下钻、青少年模式裁剪 nsfw）与 `POST /api/recommendations`（确定性简单评分：共现 → 语义节点上下文 → 个人使用频率 → post_count 先验，无 embedding/LLM）。
- **Tag Assistant 独立组件**：`static/tag-assistant.js/.css`，零依赖原生 JS，四入口（推荐/目录/搜索/收藏），消费 PromptBridge（`getDocument/getActiveTarget/subscribe/dispatch`），只发 `ADD_TAG`，不维护第二份权威状态；配套 `tests/test_tag_assistant.mjs`（22 项基线）。
- **Visual Prompt Builder 独立组件**：`static/visual-builder.js/.css`，语义卡片 + chip 编辑方式编辑 Prompt，Base/Character 多角色工作区，全套 action（ADD_TAG/REMOVE_TAG/SET_WEIGHT/MOVE_SECTION/ADD_CHARACTER/REMOVE_CHARACTER/RENAME_CHARACTER）；配套 `tests/test_visual_builder.mjs`（25 项基线）。
- **UI 重构收尾**（`outputs/overview.md`）：一级工作流统一为「生图 / 标签超市 / 图库 / 设置」；收藏/最近/导入/模板/Prompt 历史归入二级栏；视觉统一灰白 + 砖红；移动端 390×844 实测通过。
- **测试基线扩充**：README 当前声明（非付费）——Python **134 项**、Node 服务端与 Prompt Compiler **74 项**、app.js 纯函数 **24 项**、Tag Assistant **22 项**、Visual Builder **25 项**，覆盖搜索/拼音/导入/Bundle/Snapshot/图库/NovelAI payload/串行批次/取消/翻译/设置/推荐语义导航/两组件。

**验证情况**：README 基线数（Python 134、Node 74、前端纯函数 24+22+25）为当前工作区状态声明；`outputs/overview.md` 记录 Python 86 passed 与双端浏览器实测。**注意**：这些基线数字分别对应不同时点（86 为 08-25 UI 收尾时点，134 为当前工作区），不可混为一谈。

---

## 当前能力全景

| 功能模块 | 关键文件 |
| --- | --- |
| FastAPI 后端入口（标签/搜索/导入/图库/设置/翻译 API，自动拉起 Node） | `app.py` |
| SQLite 数据底座（tags / user_tags / tag_section_override / tag_bundle / prompt_snapshot / gallery 等表） | `db.py` |
| 搜索 V2（分层匹配、词序无关、拼音、别名解析、分类浏览） | `search.py` |
| 数据导入与清理（Danbooru 同步、中文名、别名、受限分类、目录构建、拼音回填） | `importer/`（sync_danbooru / import_taxonomy / import_danbooru_zh / import_aliases / import_restricted / build_catalog / pinyin_util / backfill_pinyin） |
| Prompt 整理（固定 10 分区、本地确定性分类、导入解析、NovelAI 语法导出） | `prompt/`（sections / composer / import_parser / novelai_export） |
| 语义导航与确定性推荐（Base/Character 概念树、seed 上下文、共现评分） | `config/prompt_navigation.json`（待提交）+ `app.py` 路由 `/api/catalog/semantic`、`/api/recommendations` |
| Node 生图代理层（8787 统一入口、静态面板、FastAPI 白名单代理、SSE、官方 API 生图、串行批次、资产落盘） | `server/`（server.mjs / novelai-provider.mjs / generation-request.mjs / api-batch.mjs / store.mjs / start-nai.sh） |
| 前端（无框架原生 JS；含 Prompt 编译器与文档契约） | `static/`（index.html / app.js / app.css / prompt-compiler.js / prompt-document.js） |
| Tag Assistant 独立组件（推荐/目录/搜索/收藏四入口） | `static/tag-assistant.js` + `static/tag-assistant.css`（待提交） |
| Visual Prompt Builder 独立组件（语义卡片 + chip 编辑） | `static/visual-builder.js` + `static/visual-builder.css`（待提交） |
| 例图 / 图库 / 预缓存 | `import_nai.py`、`imageutil.py`、`precache.py` |
| 启动与诊断（macOS 常驻、故障排查、环境变量示例） | `install-launchagent.sh`、`标签超市-诊断.command`、`.env.example` |
| 测试（Python 单测 + Node 组件/路由/编译器测试） | `tests/`（20 个文件） |

---

## 数据资产说明

| 文件 / 目录 | 用途 | 备注 |
| --- | --- | --- |
| `data/tags.sqlite` | 主标签库（约 28 MB，含 tags / tag_aliases / taxonomy_map / favorites / recent_tags / presets / tag_catalog / tag_bundle / gallery 等表） | 当前主库，最后更新 08-26 13:53 |
| `data/danbooru_zh.sqlite` | Danbooru 中文名映射库（约 31 MB） | 08-24 生成 |
| `data/tag_catalog.json` | 目录树种子 | — |
| `data/taxonomy_seed.json` / `data/nsfw_taxonomy.json` | 分类种子 / NSFW 分类 | — |
| `data/navigation.json` | 旧导航数据 | — |
| `data/zh_aliases.json` / `data/zh_characters.json` | 中文别名 / 中文角色名 | — |
| `data/label_mart.db` | 「标签超市库」 | **0 字节空文件**（08-26 03:12 创建）；与 `tags.sqlite` 的分工**待核实**，当前实际数据均在 `tags.sqlite` |
| `data/gallery/` | 图库图片（NAI 历史导出解包 + `nai_generated/` 本工具生成结果） | 最早导出 2026-06-13 |
| `data/nai_packages/` | 待导入图包 zip（2026-06-13 / 07-13 / 08-24 三批） | 原始素材 |
| `library/library.db` + `library/assets/` | Node 层生成资产索引与落盘 | 08-26 更新 |
| `config/model_overlays.json` | 模型能力语义覆盖 | 随仓库维护 |
| `config/app_settings.json` | 本机应用设置（端口等） | 已 gitignore，不随仓库 |
| `config/prompt_navigation.json` | 语义导航骨架 | **未提交**（untracked） |
| `static/thumbs/`、`static/novelai-examples/` | 标签缩略图 / 例图缓存 | gitignore，按需重建 |

---

## 已知状态与说明

- **git 历史覆盖范围**：18 个提交，**2026-08-24 ~ 2026-08-26**（reflog 与 `git log` 一致，无 08-23 提交；任务材料中「08-23→08-25」口径与真实 git 不符，已以 git 为准修正）。工作区存在「reset: moving to HEAD」记录（`51c0b7b`、`a2e4bc6` 处）及同主题双提交（`3ae9b3c` 与 `66ac753`），表明开发期有过提交整理/重提，未做合并压缩。
- **工作区未提交改动**：见阶段⑤。`README.md`、`app.py`、`static/` 三件套为「已修改未提交」，语义导航、两组件、`配置指南.md` 及 4 个测试文件为「未跟踪新增」。因此 README 中后段功能（Tag Assistant、Visual Builder、语义导航、schema v2、Python 134 基线）目前**只存在于工作区，不在任何 git 提交中**。
- **测试基线口径**：08-25 修复轮为 Python 86 / Node 19；`outputs/overview.md`（08-25）为 Python 86；README 当前（工作区）为 Python 134 / Node 74 / 前端 24+22+25。三者对应不同时点，引用时需注明。
- **label_mart.db 与 tags.sqlite 分工**：`label_mart.db` 当前为 0 字节空文件，实际标签数据在 `tags.sqlite`。二者关系与历史意图**待核实**。
- **数据隐私**：本机设置（NovelAI Token、百度翻译凭据、代理等）存放在仓库外 `~/.workbuddy/tags-market-settings.json`，凭据不回显、文件权限 0600；仓库内仅留 `.env.example` 占位。本机环境细节已从文档与启动脚本中抽离（commit `a2e4bc6` / `8c3a58a`）。
- **付费边界**：所有 NovelAI 生图 / 例图会消耗真实 Anlas，需用户明确确认；未确认的例图请求被 Python/Node 双层 Anlas Gate 以 428 阻断。历史开发中未执行真实付费生图。

---

## 后续方向（展望）

> 以下均为**展望，非已落地**，依据 README 中「预留/待集成」表述与当前未提交状态合理推断，不代表已排期。

- **Text/Visual 模式切换**：README 已在生图工作台预留 `#prompt-mode-switch` 挂载点并说明「Text/Visual 模式切换由集成方实现，本次不包含」——Visual Builder 组件已就绪，但模式切换入口尚未集成到主 UI（展望：将 `#tag-assistant-root`、`#visual-prompt-root` 挂载到生图工作台，完成双模式编辑）。
- **付费链路实测**：尚未执行真实付费生图，单张/多张付费 Gate、实网 402/429 限流路径未经真实触发（展望：付费冒烟与限流友好提示）。
- **未提交成果入库**：阶段⑤全部未提交改动（语义导航、两组件、schema v2、UI 收尾、新增测试）等待一次正式提交固化（展望：提交后即成为 git 历史的一部分）。
- **标签库持续扩充**：`/api/sync`、`/api/sync-hot` 支持从 Danbooru 增量同步，「更新标签库」为长期数据维护动作（展望：按需同步并回填拼音）。
- **多角色与模板沉淀**：Multi-Character 角色位置、TagBundle/Preset 已具备，但角色库/模板市场的沉淀与应用场景仍以个人使用为主（展望：进一步积累复用组合）。
- **许可证**：README 注明仓库当前未包含 LICENSE，作者明确许可前仅供个人学习参考（展望：补充开源许可）。