# 配置指南:NovelAI 提示词标签超市

本指南面向本机部署与日常运维,内容全部来自仓库内实际文件与代码(来源见文末「事实来源」)。文中所有 `<尖括号>` 内容均为占位符,不代表任何真实密钥。

## 1. 项目概览与架构

本地优先的 NovelAI Prompt 工作台:搜索标签 → 加入购物车 → 分区与权重整理 → 调用 NovelAI 官方 API 生图 → 图库管理与恢复。

技术栈:**FastAPI + SQLite(后端)+ 原生 JavaScript 前端 + Node.js 代理层**。两个服务都只监听 `127.0.0.1`,单机工具,无认证 / TLS。

```text
浏览器(static/ 原生 JS)
   │
   ├── Node 代理  127.0.0.1:8787   server/server.mjs
   │       静态面板 + Python 路由反向代理
   │       NovelAI 官方 API 生图(Token 只在本层持有)
   │       SSE 进度事件 / 资产保存(library/)/ 图库回写
   │
   └── FastAPI 后端  127.0.0.1:8123   app.py / db.py / search.py / importer/ / prompt/
           标签数据、搜索、导入、图库索引、设置、翻译(SQLite:data/tags.sqlite)
```

- **统一面板入口:http://127.0.0.1:8787**(Node 服务,含前端与 API 代理,推荐)。
- **FastAPI 直连:http://127.0.0.1:8123**(标签 / 图库 / 设置 API)。
- 前端统一从 8787 入口访问;Node 把标签 / 例图 / 图库 / 设置请求转发给 FastAPI。
- NovelAI Persistent API Token 只由本地 Node 服务读取,不返回浏览器、不写入项目源码。

## 2. 环境要求(macOS 前置条件)

| 依赖 | 版本 | 说明 |
| --- | --- | --- |
| Python | 3.10+ | 本机实测 3.12.14 可运行 |
| Node.js | 22.5+(package.json engines) | 生图依赖 Node 层 |

Node 三种可用方式(优先级从高到低,见 `app.py` 的 `_node_executable()` 与 `server/start-nai.sh`):

1. 环境变量 `NODE_BIN` 指向可执行文件;
2. 托管 Node:`$WORKBUDDY_HOME/binaries/node/versions/22.12.0/bin/node`(start-nai.sh 与 app.py 均检查该固定版本路径);
3. 系统 PATH 中的 `node`。

> 若本机没有 Node 22+,安装方式任选:安装到 `$WORKBUDDY_HOME/binaries/node/versions/22.12.0/bin/`、设置 `NODE_BIN`,或通过 Homebrew 等安装并加入 PATH。

可选账号(按需):
- NovelAI:Persistent API Token(生图与标签例图必需)。
- Danbooru:登录名 + API Key(「更新标签库」与抓取例图,可选)。
- 百度翻译:APP ID + Secret(手动翻译,可选)。
- 出网代理(可选,国内网络访问 Danbooru / NovelAI 常需)。

生图链路为官方 API-only(`https://image.novelai.net`),不需要 Edge / CDP / 浏览器登录。

## 3. 安装依赖

```bash
python -m venv .venv
source .venv/bin/activate          # Windows: .venv\Scripts\activate
python -m pip install -r requirements.txt
npm install
```

- `requirements.txt` 内容:`fastapi`、`uvicorn[standard]`、`python-multipart`、`Pillow`、`pypinyin`。
- `package.json` 运行时依赖仅 `undici`(代理支持)与 `jszip`(官方响应解压);`engines.node >= 22.5.0`。
- `npm install` 产物落在仓库根目录 `node_modules/`(已 gitignore),无前端构建步骤。
- Node 层通过 `require("undici")` / `require("jszip")` 从 `server/` 向上解析到仓库根目录的 `node_modules/`;`start-nai.sh` 与 `app.py` 会额外把 `NODE_PATH` 指向 `$WORKBUDDY_HOME/binaries/node/workspace/node_modules` 作为可选 fallback,不是运行前提。

## 4. 首次使用配置

1. 启动后首次运行会自动初始化 `data/` 并种子化标签库(`app.py` lifespan 中的 `ensure_seeded()`:导入 taxonomy / 中文别名 / 受限分类 / 目录;受限 taxonomy 文件缺失时自动跳过,不阻塞启动)。
2. 打开面板(http://127.0.0.1:8787)→ 设置页:
   - **NovelAI Persistent API Token**:生图与例图必需,只保存在本机设置文件,也可用环境变量 `NOVELAI_API_KEY`。
   - 可选:Danbooru 账号 / API Key、百度翻译凭据、代理、青少年模式、缓存上限、例图提示词模板。
3. 「更新标签库」从 Danbooru 增量同步更多标签(`POST /api/sync`、`/api/sync-hot`)。

## 5. 日常启动与访问

```bash
python app.py        # 需在激活的 .venv 中,或直接 .venv/bin/python app.py
```

启动行为(源码事实):

- FastAPI 监听 `127.0.0.1:8123`(host/port 取自 `config/app_settings.json`)。
- 启动时自动检测并拉起 Node 服务(`127.0.0.1:8787`),无需另开终端;退出 Python 应用时本次自动启动的 Node 子服务一起退出。
- 若 8787 已由用户手工启动,则直接复用且不会关闭它。
- 多实例防互杀:某实例因端口被占而启动失败、即将退出时,不会误杀仍在健康服务的 node(owner 记录在 `.workbuddy/runtime/novelai-service.pid`)。
- 默认开启热重载(Python uvicorn reload + Node `--watch`);稳定运行请先设置 `TAGS_MARKET_RELOAD=0` 再启动。
- Node 自动启动失败时,日志在 `.workbuddy/runtime/novelai-service.log`;如需彻底关闭自动启动,设置 `TAGS_MARKET_AUTOSTART_NAI=0`。

访问入口:
- 统一面板:http://127.0.0.1:8787(推荐)
- FastAPI 直连:http://127.0.0.1:8123

## 6. 环境变量总表

**项目不会自动读取 `.env` 文件**。这些变量需要在启动进程前 `export`(或在启动脚本 / LaunchAgent plist 中设置),`.env.example` 只作示例。环境变量优先于设置文件中的同名项。

| 变量 | 默认值 | 作用 |
| --- | --- | --- |
| `WORKBUDDY_HOME` | `~/.workbuddy` | 覆盖本机 WorkBuddy 目录(用户设置文件、托管 Node 所在目录) |
| `NODE_BIN` | 空 | 指定 Node.js 22+ 可执行文件路径 |
| `NOVELAI_API_KEY` | 空 | NovelAI Persistent API Token(优先于设置文件) |
| `NAI_PROXY_URL` | 空 | 出网代理地址(优先于设置文件 `proxy_url`;留空 = 使用设置文件代理或不走代理) |
| `DANBOORU_LOGIN` | 空 | Danbooru 登录名(同步时构造 Basic Auth,优先于设置文件) |
| `DANBOORU_API_KEY` | 空 | Danbooru API Key(优先于设置文件) |
| `BAIDU_TRANSLATE_APPID` | 空 | 百度翻译 APP ID(优先于设置文件) |
| `BAIDU_TRANSLATE_SECRET` | 空 | 百度翻译密钥(优先于设置文件) |
| `TAGS_MARKET_RELOAD` | `1`(开启) | 设为 `0` / `false` / `no` 关闭 Python + Node 热重载 |
| `TAGS_MARKET_AUTOSTART_NAI` | `1`(开启) | 设为 `0` / `false` / `no` 关闭 Node 服务自动启动 |

Node 层专用(手工启动 `server.mjs` 时可用,`app.py` 自动启动时会自行设置):

| 变量 | 默认值 | 作用 |
| --- | --- | --- |
| `PYTHON_APP_URL` | `http://127.0.0.1:8123` | Node 反向代理的 Python 后端地址 |
| `PYTHON_GALLERY_URL` | `<PYTHON_APP_URL>/api/gallery/item` | 生图资产回写 Python 图库的接口 |
| `LIBRARY_DIR` | 仓库根 `library/` | Node 资产存储目录(assets + library.db) |
| `NODE_PATH` | 空 | 可选 fallback:托管 `node_modules` 目录 |

说明:
- `DANBOORU_LOGIN` 在代码(`importer/sync_danbooru.py`)中真实读取,但 README 与 `.env.example` 未列出,此处补齐。
- 变量优先级(代码事实):`NOVELAI_API_KEY` / `NAI_PROXY_URL` / `BAIDU_TRANSLATE_*` 环境变量 > 用户设置文件;Danbooru 凭据:环境变量 > 用户设置文件 > `config/app_settings.json`。

## 7. config/app_settings.json 字段说明

路径:`config/app_settings.json`(本机文件,已被 gitignore,不提交)。字段与消费方:

| 字段 | 说明 | 实际消费方 |
| --- | --- | --- |
| `host` / `port` | FastAPI 监听地址 / 端口(默认 `127.0.0.1:8123`) | `app.py`(uvicorn 启动、PYTHON_APP_URL 拼装) |
| `db_path` | 数据文件路径(默认 `data/tags.sqlite`) | 当前未被消费,`db.py` 固定使用 `data/tags.sqlite` |
| `taxonomy_seed_path` / `zh_aliases_path` | 种子数据路径 | 当前未被消费,`importer/` 固定使用 `data/taxonomy_seed.json` / `data/zh_aliases.json` |
| `restricted_taxonomy_path` | 受限 taxonomy 文件路径(相对仓库根) | `importer/import_restricted.py`;查找顺序:本字段 → `data/adult_curated_taxonomy_seed.json` → 仓库上级目录 |
| `proxy.enabled` / `proxy.url` | Danbooru 同步的兜底代理(用户设置未配置代理时使用) | `importer/sync_danbooru.py` |
| `danbooru.base_url` / `page_limit` / `user_agent` / `login` / `api_key` | Danbooru 同步参数与凭据 | `importer/sync_danbooru.py` |
| `hot_sync.general` / `character` / `artist` / `copyright` / `meta` | 热门同步各分类条数上限 | `importer/sync_danbooru.py` 的 `run_hot_sync()` |

注意:
- `danbooru.login` / `danbooru.api_key` 属于敏感凭据;优先放到用户设置文件或环境变量,而不是写进 `config/app_settings.json`。
- 该文件被 gitignore,不会随仓库提交;若从别处复制仓库,需要自行准备。

## 8. 用户设置文件与敏感信息位置

- 路径:`$WORKBUDDY_HOME/tags-market-settings.json`(默认 `~/.workbuddy/tags-market-settings.json`,`WORKBUDDY_HOME` 可覆盖,与 `server/start-nai.sh` 一致)。
- 由设置页写入,文件权限 `0600`;凭据不回显(API 只返回 `has_danbooru_api_key` / `novelai_configured` / `baidu_translate_configured` 等布尔状态)。
- 支持字段(即 `DEFAULT_USER_SETTINGS` 与 `UserSettingsBody`):

| 字段 | 默认值 | 说明 |
| --- | --- | --- |
| `adolescent_mode` | `true` | 青少年模式,隐藏 NSFW 目录与语义搜索结果 |
| `cache_limit_mb` | `1024` | 缩略图缓存上限(0–102400),超限按修改时间淘汰最旧文件 |
| `proxy_enabled` / `proxy_url` | `true` / 空 | 代理开关与地址 |
| `danbooru_login` / `danbooru_api_key` | 空 | Danbooru 凭据 |
| `novelai_api_token` | 空 | NovelAI Persistent API Token |
| `novelai_batch_max_count` | `6` | 单批次张数上限(1–6,强制钳制) |
| `novelai_example_credit_warning` | `true` | 生成例图前是否提示消耗 Anlas |
| `novelai_example_prompt_template` | `{tag}, {rating}, masterpiece, best quality, very aesthetic, absurdres` | 例图提示词模板,必须包含 `{tag}` |
| `baidu_translate_appid` / `baidu_translate_secret` | 空 | 百度翻译凭据 |

> 注意:`importer/sync_danbooru.py` 读取用户设置文件时使用硬编码的 `~/.workbuddy/tags-market-settings.json`(不遵循 `WORKBUDDY_HOME`)。若通过设置文件配置了 Danbooru 凭据,请保持默认路径,不要用 `WORKBUDDY_HOME` 换目录。

## 9. 数据目录与备份建议

| 路径 | 内容 | 是否可再生 |
| --- | --- | --- |
| `data/tags.sqlite`(及 `-wal` / `-shm`) | 主数据库:标签、收藏、最近使用、快照、图库索引、设置等 | 否,需备份 |
| `data/danbooru_zh.sqlite` | Danbooru 中文名参考库(只读种子) | 可重新导入 |
| `data/taxonomy_seed.json`、`zh_aliases.json`、`zh_characters.json`、`nsfw_taxonomy.json`、`tag_catalog.json`、`navigation.json` | 种子 / 辅助数据 | 仓库 / 重新生成 |
| `data/gallery/` | Python 图库图片文件 | 否,需备份 |
| `library/` | Node 资产库(`library.db` + `assets/`,已 gitignore) | 否,需备份 |
| `static/thumbs/` | 标签缩略图缓存 | 是,按需懒抓取,设置页可清理 |
| `static/novelai-examples/` | 例图缓存 | 是,重新生成,设置页可清理 |
| `待清理/图库/` | 图库软删除回收区(已 gitignore) | 清理前确认 |
| `.workbuddy/runtime/` | Node 自动启动日志与 pid(仓库内) | 是 |
| `~/Library/Logs/TagSupermarket/` | LaunchAgent 的 stdout / stderr 日志 | 是 |

备份建议:
- 停服后直接复制 `data/tags.sqlite`(+ 同目录 `-wal` / `-shm` 若存在)、`library/`、`data/gallery/` 即可完整恢复。
- 运行中备份可用 macOS 自带 `sqlite3` 在线备份命令:`sqlite3 data/tags.sqlite ".backup '<备份路径>/tags.sqlite'"`。
- 缓存目录与日志目录无需备份。

## 10. 凭据与安全注意事项

- **NovelAI Token**:只由本地 Node 层持有,浏览器不接触。保存在用户设置文件(0600)或环境变量 `NOVELAI_API_KEY`。示例(占位符):`export NOVELAI_API_KEY=<你的PersistentApiToken>`。
- **Danbooru**:登录名 + API Key 组成 Basic Auth,仅用于同步标签 / 抓例图;示例:`export DANBOORU_LOGIN=<账号>`、`export DANBOORU_API_KEY=<密钥>`。
- **百度翻译**:仅手动点击时请求,不会自动上传词库;示例:`export BAIDU_TRANSLATE_APPID=<appid>`、`export BAIDU_TRANSLATE_SECRET=<secret>`。
- **代理**:`NAI_PROXY_URL` 环境变量优先于设置文件 `proxy_url`;Node 层代理依赖 `undici`(即 `npm install` 的产物)。
- 两个服务都只绑定 `127.0.0.1`;Node 层对非 loopback Origin 的 `/api` 请求返回 403。
- 运维警示:
  - 不要把真实 Token / 密钥填入 `.env.example` 或任何会提交的文件;`config/app_settings.json` 同样禁止含真实凭据。
  - 检查 `git status` 确认没有误提交 `~/.workbuddy`、`config/app_settings.json`、`key.docx`、`*.key` 等。
  - 生图与标签例图会消耗 NovelAI 账户 Anlas,所有付费请求均需在界面明确确认。

## 11. 单独启动 Node 层(可选)

```bash
./server/start-nai.sh
```

- 启动 Node 联动层(8787,官方 API-only);脚本为 zsh,使用 `NODE_BIN`(或托管 / PATH 中的 Node),未找到时提示「未找到 Node.js 22+」。
- 生图走官方 API,无需 Edge / CDP / 浏览器登录。
- Node 层只负责面板静态文件、API 代理与生图;标签 / 图库 / 设置等 Python 路由仍需 FastAPI 在 8123 运行(`PYTHON_APP_URL` 默认 `http://127.0.0.1:8123`)。
- 手工启动时可覆盖 `LIBRARY_DIR` / `PYTHON_APP_URL` / `PYTHON_GALLERY_URL`(见第 6 节)。

## 12. LaunchAgent 常驻与日志

```bash
bash install-launchagent.sh          # 安装并加载
bash install-launchagent.sh --print  # 仅打印将生成的 plist,不写盘、不加载
```

- 前置条件:项目内存在 `.venv/bin/python`(脚本会校验并给出提示)。
- 生成 `~/Library/LaunchAgents/com.tagsupermarket.server.plist`:`RunAtLoad` + `KeepAlive`,执行 `项目路径/.venv/bin/python 项目路径/app.py`,`TAGS_MARKET_RELOAD=0`(稳定模式),PATH 含 `/usr/local/bin` 与 `/opt/homebrew/bin`。
- 日志:`~/Library/Logs/TagSupermarket/stdout.log` 与 `stderr.log`。
- 安装完成提示的网页入口为 http://127.0.0.1:8123;关闭窗口 ≠ 停止服务。
- 较新 macOS 上 `launchctl load` 可能失败,脚本会提示改用:`launchctl bootstrap gui/$(id -u) <plist>`。
- **重要**:LaunchAgent 不继承你 shell 里的环境变量。需要 Token 等时,要么写入用户设置文件,要么手工把变量加进 plist 的 `EnvironmentVariables`(如 `NODE_BIN`、`NOVELAI_API_KEY`)。
- 卸载:`launchctl unload <plist>`(或 `bootout`)后删除 plist 文件。

故障排查工具:`./标签超市-诊断.command`(Finder 双击亦可)。它只读检查三项:LaunchAgent 加载状态、8123 端口监听、日志文件;菜单 `r` 重新加载 LaunchAgent、`o` 打开网页、`q` 退出。不做自动修复、不做进程强杀。

## 13. 测试与诊断

测试命令(已在本机验证通过):

```bash
# Python 后端(实测 134 项全过)
.venv/bin/python -m unittest discover -s tests

# Node 服务端 + Prompt Compiler(实测 82 项全过)
# 注意:需要 --experimental-sqlite(store.test.mjs 使用 node:sqlite),package.json 的 npm test 已包含该参数
node --experimental-sqlite --test server/*.test.mjs tests/test_prompt_compiler.mjs
npm test                        # 等价于 node --experimental-sqlite --test server/*.test.mjs tests/*.mjs(服务端测试 + 全部 P0 前端回归测试)

# 前端 / 帮助函数(实测全过)
node --test tests/test_app_helpers.mjs tests/test_prompt_document.mjs tests/test_tag_assistant.mjs

# 语法检查
node --check static/app.js
node --check static/tag-assistant.js
node --check server/server.mjs
```

> 注:README 中写的是 `node --test server/*.test.mjs tests/test_prompt_compiler.mjs`(缺 `--experimental-sqlite`),在 Node 22 下 store 相关测试会报 `ERR_UNKNOWN_BUILTIN_MODULE`;请以上面带参数的命令为准。

诊断端点与日志:
- `GET http://127.0.0.1:8787/api/status` — Node 服务状态(mode: api-only)。
- `GET http://127.0.0.1:8787/api/novelai/status` — 官方 API 探针(只读验证 Token / 代理 / 网络,不消耗 Anlas)。
- `GET /api/batches`、`GET /api/assets`、`GET /api/jobs` — 诊断用批次 / 资产 / 任务列表。
- Node 自动启动日志:仓库内 `.workbuddy/runtime/novelai-service.log`。
- LaunchAgent 日志:`~/Library/Logs/TagSupermarket/stdout.log`、`stderr.log`。

## 14. 常见故障排查与运维注意事项

| 现象 | 排查 |
| --- | --- |
| 启动报「未找到 Node.js 22+」 | 设置 `NODE_BIN` 指向 node 22+,或把 node 放到 `$WORKBUDDY_HOME/binaries/node/versions/22.12.0/bin/` |
| Node 自动启动失败 | 查看 `.workbuddy/runtime/novelai-service.log`;临时关闭用 `TAGS_MARKET_AUTOSTART_NAI=0`,或单独 `./server/start-nai.sh` |
| 8123 / 8787 端口被占 | 已有实例在运行(多实例防互杀不会误杀健康 node);确认后关闭冗余实例 |
| 热重载导致不稳定 | 稳定运行前 `export TAGS_MARKET_RELOAD=0` |
| NovelAI 401 / 403 | Token 无效或已失效(provider 明确报「Token 无效或已失效」);重新生成 Persistent API Token |
| NovelAI 402 | Anlas 余额不足 |
| NovelAI 429 | 请求频率受限,稍后重试 |
| 生图报代理相关错误 | 确认 `NAI_PROXY_URL` 或设置文件代理可用;确认已 `npm install`(undici) |
| Danbooru 同步失败 | 多为网络 / 代理不可达;同步为手动触发、断网优雅报错,不崩服务 |
| 缩略图 / 例图缓存膨胀 | 设置页查看缓存统计并清理;缓存可再生,不影响数据 |
| LaunchAgent 加载失败 | 用 `launchctl bootstrap gui/$(id -u) <plist>`;或重跑 `install-launchagent.sh` |
| 误删图库 | 删除是软删除:先移入 `待清理/图库/`,再移除活动索引;未清空回收区前可找回 |

其他注意事项:
- 批次为 1–6 张严格串行,单并发;任意一张失败即停止后续请求。
- 删除图库目录属于软删除,最终清理前先确认 `待清理/图库/`。
- 升级 / 大改前先按第 9 节备份 `data/tags.sqlite`、`library/`、`data/gallery/`。
- 本项目与 NovelAI 官方无关联;标签数据来自 Danbooru,请遵守相应平台使用条款。

## 15. 事实来源(Source of Truth)

本指南基于对仓库实际文件的检查编写:README.md、.env.example、config/app_settings.json、config/model_overlays.json、package.json、requirements.txt、app.py、db.py、search.py、importer/(sync_danbooru.py、import_restricted.py、import_taxonomy.py、import_aliases.py 等)、server/(server.mjs、novelai-provider.mjs、start-nai.sh)、install-launchagent.sh、标签超市-诊断.command、.gitignore。

- 功能特性与完整 API 列表以 README.md 为权威;本指南聚焦配置、部署与运维。
- 已知与 README 的差异(本指南已按代码事实修正):(1) Node 测试命令需 `--experimental-sqlite`;(2) `DANBOORU_LOGIN` 为代码支持但 README 未列出的环境变量;(3) `app_settings.json` 的 `db_path` / `taxonomy_seed_path` / `zh_aliases_path` 三个字段当前未被代码消费,`db.py` 与 importer 使用 `data/` 下固定路径。
- 若本指南中的任何命令与仓库实际行为不符,请以代码为准并向维护者反馈,而不是反向修改 README。